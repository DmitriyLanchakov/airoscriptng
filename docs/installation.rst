============
Installation
============

At the command line::

    $ easy_install airoscript-ng

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv airoscript-ng
    $ pip install airoscript-ng